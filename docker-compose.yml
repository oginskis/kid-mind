services:
  chromadb:
    image: chromadb/chroma:1.5.2
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/data
    environment:
      - CHROMA_HOST=0.0.0.0
      - CHROMA_PORT=8000

volumes:
  chroma-data:
